# Pip and Packages

# Pip = Python`s Package Manager and installer
# pip -v = shows the commands for pip
# pip -V = shows the version we have installed


# import requests
#
# request_bbc = requests.get("https://www.bbc.co.uk")
#
# print(request_bbc.status_code) # outputs 200, because 200 = everything is fine, the site is up and running
# print(request_bbc.content) # outputs the whole Html code for the website


# Creating our own packages

# Packages ways of structuring programs in a certain form so they can be used in other programs

#create a folder fr the package
#create a folder within the package folder for the app

